# Declare two doubles `A` and `B` to setup variable addition
A <- 10
B <- 5

# Add two variables and assign result to third variable `C`
C <- A + B

# Print value of variable `C` to console
C

# Subtract two variables and assign result to third variable `D`
D <- A - B
D

# Declare two doubles `var1` and `var2` to setup variable division
var1 <- 2.5
var2 <- 4

# Divide two variables and assign result to third variable `result_div` and print
result_div <- var1 / var2
result_div

# Multiply two variables and assign result to variable `result_prod` and print
result_prod <- var1 * var2
result_prod

# Attain square root of `var2` and assign result to variable `answer` and print
answer <- sqrt(var2)
answer

# Create concatenated message string and print to console
greeting <- "Hello"
name <- "Bob"
message <- paste(greeting, name)
message
